// Central API base URL for all frontend fetch calls.
// Set NEXT_PUBLIC_API_URL in your .env.local (dev) or Vercel env vars (production).
export const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001';
